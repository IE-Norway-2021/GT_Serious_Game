// Create a noise function
// Noise noise = new Noise();

// Generate a random pattern of heights using the noise function
// float[,] heights = noise.GenerateNoise(width, height);

// Loop through all of the elements in the array
/* for (int x = 0; x < width; x++) {
    for (int y = 0; y < height; y++) {
        // Get the height at this point
        float height = heights[x, y];

        // Normalize the height value to be between 0 and 1
        height = (height + 1) / 2;

        // Set the normalized height value in the array
        heights[x, y] = height;
    }
} */